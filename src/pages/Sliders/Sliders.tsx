import React from 'react';
import SlidersContainer from "../../components/Containers/Sliders-container";

const Sliders: React.FC = () => {
    return (
        <div>
        <SlidersContainer/>
        </div>
    );
};

export default Sliders;